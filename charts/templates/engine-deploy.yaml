apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-engine
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-engine
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/component: engine
spec:
  replicas: {{ .Values.engine.replicaCount }}

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: {{ .Release.Name }}-engine

  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-engine
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/name: {{ .Release.Name }}
        app.kubernetes.io/component: engine
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
        {{- if .Values.engine.apparmor.enabled }}
        container.apparmor.security.beta.kubernetes.io/{{ .Release.Name }}-engine: runtime/default
        {{- end }}

    spec:
      serviceAccountName: {{ .Release.Name }}-sa
      automountServiceAccountToken: false

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: {{ .Release.Name }}-engine

      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: {{ .Release.Name }}-engine
          image: {{ .Values.engine.image.repository }}:{{ .Values.engine.image.tag }}
          imagePullPolicy: {{ .Values.engine.image.pullPolicy }}

          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-config
            {{- if .Values.config.existingSecret }}
            - secretRef:
                name: {{ .Values.config.existingSecret }}
            {{- end }}

          resources:
            requests:
              cpu: {{ .Values.engine.resources.requests.cpu }}
              memory: {{ .Values.engine.resources.requests.memory }}
            limits:
              cpu: {{ .Values.engine.resources.limits.cpu }}
              memory: {{ .Values.engine.resources.limits.memory }}

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            privileged: false
            capabilities:
              drop: ["ALL"]
